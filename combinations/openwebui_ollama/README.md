

# Open WebUI + Ollama

Запуск LLM с веб-интерфейсом через библиотеки:
- [**Open WebUI**](https://github.com/open-webui/open-webui) - веб-интерфейс для работы с LLM
- [**Ollama**](https://github.com/ollama/ollama) - запуск LLM моделей


## 🐳🚀 Быстрый старт

Клонирование репозитория
```
git clone https://github.com/sergey21000/webui-llm-examlples
cd webui-llm-examlples/openwebui_ollama
```

Скопировать файл `.env` с переменными окружения
```sh
cp env.example .env
```
Редактировать переменные окружения в файле `.env` при необходимости

Запуск сервисов
- Запуск с поддержкой CPU
  ```sh
  docker compose up -d
  ```
- Запуск с поддержкой CUDA
  ```sh
  docker compose -f compose.cuda.yml up -d
  ```

Загрузка моделей для Ollama  
https://ollama.com/library
```
docker exec -it ollama ollama pull gemma3:4b
```
Список загруженных моделей Ollama
```
docker exec -it ollama ollama list
```

Проверить статус сервисов
```sh
docker compose ps
```
Просмотр логов запуска сервисов
```
docker compose logs -f
```

По умолчанию сервисы будут доступны по адресам:
- Open WebUI: http://127.0.0.1:3000
- Ollama API: http://127.0.0.1:11434

При первом запуске Open WebUI ввести почту и пароль администратора (можно любые, главное запомнить)  
Или отключить авторизацию в файле `.env` перед запуском сервисов

Настроить поставщика LLM:
- Нажать на значок пользователя в левом нижнем углу экрана - `Настройки` -> `Настройки админимтратора` -> `Подключения` -> нажать `+` напротив пункта `Управление соединениями API OpenAI` -> вписать http://vllm:8000/v1 в поле URL -> `Сохранить`

В интерфейсе чата тоже нужно выбрать модель где написано `Выберите Модель`  
Отключить режим рассуждений - добавить `/no_think ` в начало запроса


---
Вариант запуска сервисов без указания `-f compose.cuda.yml`

Установка переменной `COMPOSE_FILE`
- Linux
  ```sh
  export COMPOSE_FILE=compose.cuda.yml
  ```
- Windows PowerShell
  ```ps1
  $env:COMPOSE_FILE="compose.cuda.yml"
  ```

Запуск
```sh
docker compose up -d
```
